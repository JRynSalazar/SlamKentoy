<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myslambook"
        tools:targetApi="31">

        <!-- HomeActivity: Not exported as it's used internally -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- CreateAccountActivity: Not exported as it's used internally -->
        <activity
            android:name=".CreateAccountActivity"
            android:exported="false" />


        <!-- LoginActivity: Not exported as it's used internally -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- MainActivity: This is the entry point and must be exported -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
